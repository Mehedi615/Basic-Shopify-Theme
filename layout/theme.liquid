<!DOCTYPE html>
<html lang="en">
  <!DOCTYPE html>
</html>
<html>
  <head>

    {{ content_for_header }}
    <title>{{ page.title }}</title>

  </head>

  <body>
    {{ content_for_layout }}
    {% render 'header' %}

  </body>
</html>